spring:
  application:
    name: hello-service
  cloud:
    config:
      uri: http://localhost:8040/
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    instance:
      preferIpAddress: true
